window.interop = {

    initializeBootstrap: function(selector) {

        $(function() {
            // This runs, but my animations still do not show
            //$( "#side-navigation" ).tabs({ show: { effect: "fade", duration: 400 } });
        });  

        
    }

}